# Brand assets

`datasynx-logo.png` is served at `/brand/datasynx-logo.png` and used by the
top bar, the auth pages and the favicon. Replace the file to rebrand; the
`Brand` component falls back to the wordmark when the file is absent.
