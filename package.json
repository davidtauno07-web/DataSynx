{
  "name": "datasynx",
  "private": true,
  "version": "0.1.0",
  "description": "DataSynx — multimodal data processing, structuring, compilation and export platform",
  "workspaces": [
    "backend/api",
    "frontend"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n api,worker,web -c gray,white,cyan \"npm:dev:api\" \"npm:dev:worker\" \"npm:dev:web\"",
    "dev:api": "npm run dev --workspace backend/api",
    "dev:worker": "npm run dev:worker --workspace backend/api",
    "dev:web": "npm run dev --workspace frontend",
    "build": "npm run build --workspace backend/api && npm run build --workspace frontend",
    "lint": "npm run lint --workspace backend/api && npm run lint --workspace frontend",
    "typecheck": "npm run typecheck --workspace backend/api && npm run typecheck --workspace frontend",
    "test": "npm run test --workspace backend/api && npm run test --workspace frontend",
    "db:migrate": "npm run db:migrate --workspace backend/api",
    "db:deploy": "npm run db:deploy --workspace backend/api",
    "db:generate": "npm run db:generate --workspace backend/api",
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
